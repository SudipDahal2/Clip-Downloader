version: '3.8'

services:
  web:
    build: .
    volumes:
      - .:/app
      - media_volume:/app/media
    ports:
      - "8000:8000"
    environment:
      - DEBUG=0
    restart: always

volumes:
  media_volume:
